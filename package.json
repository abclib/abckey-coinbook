{
  "name": "@abckey/coinbook",
  "version": "0.1.0",
  "description": "A Variety of Coinbook",
  "author": "abckey <hi@abckey.com>",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc --declaration",
    "build:ts": "rimraf dist && tsc --declaration",
    "build:js": "rimraf dist && webpack",
    "build:json": "rimraf dist && tsc && node script/toJson.js",
    "clear": "rimraf dist",
    "commit": "npx git-cz"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/abclib/coinbook.git"
  },
  "devDependencies": {
    "rimraf": "^3.0.0",
    "ts-loader": "^6.2.2",
    "typescript": "^3.4.5",
    "webpack": "^4.42.1",
    "webpack-cli": "^3.3.11",
    "commitizen": "^4.0.3"
  },
  "dependencies": {},
  "files": [
    "dist"
  ],
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
